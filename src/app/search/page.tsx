'use client';
import {useState} from 'react'; import Link from 'next/link'; import HubHeader from '@/components/entity/HubHeader';
export default function SearchPage(){const [q,setQ]=useState('');const [results,setResults]=useState<{type:string;name:string;href:string}[]>([]);const search=async()=>{if(!q.trim()){setResults([]);return;}const r=await fetch(`/api/search?q=${encodeURIComponent(q)}`);const j=await r.json();setResults(j.data)};return <><HubHeader eyebrow="Universal search" title="SEARCH THE MAPLE PITCH" description="Find players, teams, competitions and matches from one entity-aware search layer."/><div className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')search()}} placeholder="Search players, teams, matches..." className="min-w-0 flex-1 border border-border bg-surface px-4 py-3 text-sm outline-none focus:border-crimson"/><button onClick={search} className="bg-crimson px-5 text-xs font-black uppercase text-white">Search</button></div><div className="mt-6 space-y-2">{results.map(r=><Link key={`${r.type}-${r.href}`} href={r.href} className="flex justify-between border border-border p-4 hover:border-crimson"><span className="font-black">{r.name}</span><span className="text-[9px] font-mono uppercase text-charcoal-soft">{r.type}</span></Link>)}</div></>}
