import Link from 'next/link'; import HubHeader from '@/components/entity/HubHeader'; import SourceStamp from '@/components/entity/SourceStamp'; import {wireItems} from '@/lib/data/demo';
export default function TransferWire(){return <><HubHeader eyebrow="Movement intelligence" title="TRANSFER / MOVEMENT WIRE" description="A source-first movement feed. Confirmed, reported and speculative events should be visually distinct in the production version."/><div className="space-y-3">{wireItems.filter(x=>x.category==='transfer').map(x=><article key={x.id} className="border border-border p-5"><div className="text-[9px] font-mono uppercase text-crimson">MOVEMENT // {new Date(x.timestamp).toLocaleString('en-CA')}</div><h2 className="mt-1 font-black">{x.headline}</h2><p className="mt-2 text-sm text-charcoal-soft">{x.dek}</p><div className="mt-4 border-t border-border pt-2 text-[9px] font-mono uppercase">SOURCE // {x.source.name}</div></article>)}</div><div className="mt-6"><SourceStamp/></div></>}
