import Link from 'next/link'; import HubHeader from '@/components/entity/HubHeader'; import SourceStamp from '@/components/entity/SourceStamp'; import {wireItems} from '@/lib/data/demo';
export default function TacticalLibrary(){const items=wireItems.filter(x=>x.category==='tactical');return <><HubHeader eyebrow="Analysis // Tactics" title="TACTICAL LIBRARY" description="A durable archive for tactical explanations, match models and visual analysis. Every article should eventually cite its match and data sources."/><div className="grid gap-4 md:grid-cols-2">{items.map(x=><article key={x.id} className="border border-border p-5"><div className="text-[9px] font-mono uppercase text-crimson">TACTICAL NOTE // {new Date(x.timestamp).toLocaleDateString('en-CA')}</div><h2 className="mt-2 text-xl font-black">{x.headline}</h2><p className="mt-2 text-sm leading-6 text-charcoal-soft">{x.dek}</p><div className="mt-5 text-[9px] font-mono uppercase text-charcoal-soft">Source // {x.source.name}</div></article>)}</div><div className="mt-6"><SourceStamp/></div></>}
