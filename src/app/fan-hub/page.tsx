'use client';
import {useState} from 'react'; import HubHeader from '@/components/entity/HubHeader';
const options=['CPL','NSL','Provincial','National Teams'];
export default function FanHub(){const [choice,setChoice]=useState('CPL'); const [voted,setVoted]=useState(false);return <><HubHeader eyebrow="Community // Fan Hub" title="FAN HUB" description="A community layer for polls, matchday prompts and curated fan contributions. Voting is intentionally local in this demo; production voting must move server-side."/><div className="grid gap-6 lg:grid-cols-2"><section className="border border-border p-6"><div className="text-[10px] font-mono uppercase text-crimson">Matchday poll</div><h2 className="mt-2 text-2xl font-black">Which Canadian story deserves the most attention?</h2><div className="mt-5 space-y-2">{options.map(x=><button key={x} onClick={()=>setChoice(x)} className={`w-full border p-3 text-left text-sm font-bold ${choice===x?'border-crimson bg-crimson/5':'border-border hover:border-crimson'}`}>{x}</button>)}</div><button onClick={()=>setVoted(true)} className="mt-4 w-full bg-crimson px-4 py-3 text-xs font-black uppercase text-white">{voted?'RECORDED IN DEMO MODE':'CAST DEMO VOTE'}</button></section><section className="border border-border bg-card p-6"><div className="text-[10px] font-mono uppercase text-crimson">Community standards</div><ul className="mt-4 space-y-3 text-sm text-charcoal-soft"><li>• No harassment or targeted abuse.</li><li>• Fan submissions must be attributable.</li><li>• Sponsored content is labelled.</li><li>• Production polls require server-side anti-abuse controls.</li></ul></section></div></>}
